import { S } from "./schema-index"
//import { ResolvedSchemaType, UnresolvedSchemaType } from "./schema-typing"

// const TestType = S.Object({
// 	a: S.Optional(S.String({ name: "A VALUE!" })),
// 	b: S.Array(S.Number()),
// })
// type TestType = UnresolvedSchemaType<typeof TestType>

// const TestType2 = S.Array(S.Number())
// type TestType2 = UnresolvedSchemaType<typeof TestType2>
// type TestType2R = ResolvedSchemaType<typeof TestType2>
